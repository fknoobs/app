---
description: Prefer existing $lib/components over custom HTML markup
globs: packages/{app,landing}/src/**/*.{svelte,svelte.ts}
alwaysApply: true
---

# Reuse Existing Components

Before writing any UI markup, search shared UI and existing components. Do **not** invent custom HTML when a primitive or domain component already covers the need.

## Checklist (before new markup)

1. Check **`@company-of-heroes/ui`** — Button, Skeleton, Pagination, MapImage, Leaderboard, Player, Replay, PlayerPerformance, …
2. Check **`packages/app/src/lib/components/ui/`** (re-exports + app-only: Avatar, Breadcrumb, Table, Profile, …)
3. Check domain folders — `match/`, `player/`, `lobby/`, `replay/`, `match-history/`, `widgets/`, …
4. Prefer extending via props / snippets over copying markup
5. Only write raw HTML when no existing component fits — and keep it minimal

## Prefer

```svelte
<!-- ✅ existing components -->
<script lang="ts">
	import { Button } from '$lib/components/ui/button';
	import { Badge } from '$lib/components/ui/badge';
	import { Box } from '$lib/components/ui/box';
</script>

<Box>
	<Badge>Live</Badge>
	<Button variant="primary">Join</Button>
</Box>
```

## Avoid

```svelte
<!-- ❌ hand-rolled equivalents -->
<div class="rounded border bg-card p-4">
	<span class="rounded-full bg-green-500 px-2 text-xs">Live</span>
	<button class="bg-primary px-3 py-2" onclick={...}>Join</button>
</div>
```

## Do not

- Recreate Button / Input / Dialog / Table / Badge styling with raw `<button>`, `<input>`, `<div>` + Tailwind
- Duplicate domain UI (player row, match card, lobby controls) that already lives under `$lib/components`
- Skip the search because “a quick custom div is faster”
- Hand-roll match list tables — use **`Match.ListTable`** (`match-list-table.svelte`) for tabular match rows (dashboard “Matches played today”, performance map expand, etc.); extend via props (`showMap`, `showRating`, `footer`) instead of copying `<table>` markup

## Match lists

For rows of saved lobbies / match history in a table, use **`Match.ListTable`** from `$lib/components/match` with **`Match.*`** cell primitives (`MapImage`, `Title`, `Players`, `Duration`, `Rating`, `Status`). Do not build a second table with `DataTable` or raw `<table>` unless the layout is fundamentally different (e.g. full history page with pagination controls).
