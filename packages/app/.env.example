# Local PocketBase (used by `pnpm dev` / `vite dev`)
PUBLIC_PB_URL=http://localhost:8090
PUBLIC_STEAM_API_KEY=

# Production builds (`pnpm build`) load `.env.production` instead,
# which sets PUBLIC_PB_URL=https://api.coh1stats.com
