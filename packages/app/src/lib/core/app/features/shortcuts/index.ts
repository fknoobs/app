import {
	type ShortcutSettings,
	type Shortcut,
	type FactionKey,
	type BindingScope,
	Shortcuts,
	shortcuts
} from './shortcuts.svelte';

export {
	type ShortcutSettings,
	type Shortcut,
	type FactionKey,
	type BindingScope,
	Shortcuts,
	shortcuts
};